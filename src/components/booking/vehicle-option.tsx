import { CarFront, Check, Luggage, UsersRound } from "lucide-react";
import type { Vehicle } from "@/types/booking";
import { cn } from "@/lib/utils";
export function VehicleOption({ vehicle, selected, eligible, passengers, onSelect }: { vehicle: Vehicle; selected: boolean; eligible: boolean; passengers: number; onSelect: () => void }) { return <button type="button" role="radio" aria-checked={selected} disabled={!eligible} onClick={onSelect} className={cn("relative w-full rounded-2xl border-2 p-4 text-left transition-colors", selected ? "border-amber-500 bg-[#fffaf0]" : "border-line bg-white hover:border-navy-800", !eligible && "cursor-not-allowed bg-[#f5f7f8] opacity-70")}><div className="flex gap-4"><span className="grid size-14 shrink-0 place-items-center rounded-xl bg-[#edf1f4] text-navy-800"><CarFront size={30}/></span><div className="min-w-0 flex-1"><div className="flex items-start justify-between gap-3"><div><h3 className="font-extrabold text-navy-950">{vehicle.name}</h3><p className="mt-1 text-xs text-muted">Example: {vehicle.exampleModels}</p></div>{selected && <span className="grid size-7 shrink-0 place-items-center rounded-full bg-success text-white" aria-label="Selected"><Check size={16}/></span>}</div><div className="mt-4 flex flex-wrap gap-2 text-xs font-bold text-muted"><span className="flex items-center gap-1.5"><UsersRound size={15}/>{vehicle.passengers} seats</span><span className="flex items-center gap-1.5"><Luggage size={15}/>{vehicle.luggage} bags</span><span>AC</span></div><p className={cn("mt-3 text-xs font-extrabold", eligible ? "text-navy-900" : "text-danger")}>{eligible ? "Fare and availability on request" : `Not suitable for ${passengers} passengers`}</p></div></div></button>; }
