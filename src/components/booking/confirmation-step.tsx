import Link from "next/link";
import { CheckCircle2, Clock3, Phone } from "lucide-react";
import { FaWhatsapp } from "react-icons/fa6";
import { BookingSummary } from "./booking-summary";
import { phoneHref, whatsappHref } from "@/config/site";
import type { BookingDraft, BookingReference } from "@/types/booking";
export function ConfirmationStep({ draft, result }: { draft: BookingDraft; result: BookingReference }) { return <section aria-labelledby="confirmation-title" className="text-center"><span className="mx-auto grid size-16 place-items-center rounded-full bg-[#edf7f1] text-success"><CheckCircle2 size={34}/></span><p className="eyebrow mt-5">Request received</p><h1 id="confirmation-title" tabIndex={-1} className="mt-2 text-3xl font-extrabold tracking-tight text-navy-950">Booking request received</h1><p className="mx-auto mt-3 max-w-lg text-sm leading-6 text-muted">This development request is awaiting operator confirmation. It is not a confirmed taxi booking.</p><div className="mx-auto mt-6 max-w-md rounded-2xl border border-amber-500 bg-[#fffaf0] p-5"><p className="text-xs font-extrabold uppercase tracking-wider text-muted">Development reference</p><p className="mt-2 break-all text-2xl font-extrabold tracking-wider text-navy-950">{result.reference}</p><p className="mt-2 flex items-center justify-center gap-2 text-sm font-bold text-amber-600"><Clock3 size={16}/>Awaiting confirmation</p></div><div className="mx-auto mt-6 max-w-md text-left"><BookingSummary draft={draft}/>{draft.passenger && <div className="mt-3 rounded-2xl border border-line bg-white p-5"><p className="text-xs font-extrabold uppercase tracking-[.12em] text-muted">Contact</p><p className="mt-3 text-sm font-extrabold text-navy-950">{draft.passenger.fullName}</p><p className="mt-1 text-sm text-muted">{draft.passenger.mobile}</p>{draft.passenger.email && <p className="mt-1 break-words text-sm text-muted">{draft.passenger.email}</p>}</div>}</div><div className="mt-7 flex flex-col justify-center gap-3 sm:flex-row"><Link href="/" className="btn btn-primary">Back to home</Link><a href={phoneHref} className="btn border border-line bg-white text-navy-900"><Phone size={18}/>Call</a>{whatsappHref && <a href={whatsappHref} className="btn border border-line bg-white text-navy-900"><FaWhatsapp size={18}/>WhatsApp</a>}</div></section>; }
