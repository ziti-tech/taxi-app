import type { BookingStep } from "@/types/booking";
const steps = [{ id: "trip", label: "Trip" }, { id: "ride", label: "Ride" }, { id: "details", label: "Details" }, { id: "review", label: "Review" }] as const;
export function BookingProgress({ step }: { step: BookingStep }) { const index = steps.findIndex((item) => item.id === step); if (step === "confirmation") return null; return <nav aria-label="Booking progress" className="mb-7"><p className="text-sm font-extrabold text-navy-900 md:hidden">Step {index + 1} of 4 <span className="font-semibold text-muted">· {steps[index].label}</span></p><ol className="hidden grid-cols-4 md:grid">{steps.map((item, itemIndex) => <li key={item.id} aria-current={item.id === step ? "step" : undefined} className="flex min-w-0 items-center"><span className={`grid size-8 shrink-0 place-items-center rounded-full text-xs font-extrabold ${itemIndex <= index ? "bg-navy-900 text-white" : "bg-[#e9eef2] text-muted"}`}>{itemIndex + 1}</span><span className={`ml-2 text-sm font-extrabold ${itemIndex <= index ? "text-navy-950" : "text-muted"}`}>{item.label}</span>{itemIndex < steps.length - 1 && <span className="mx-3 h-px min-w-4 flex-1 bg-line"/>}</li>)}</ol></nav>; }
