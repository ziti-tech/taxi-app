import Link from "next/link";
import { CalendarCheck, Phone } from "lucide-react";
import { phoneHref } from "@/config/site";
export function FinalBookingCta({ title = "Ready to plan your taxi?", description = "Enter your trip details, choose a suitable vehicle and send a booking request.", bookingHref = "/book" }: { title?: string; description?: string; bookingHref?: string }) { return <section className="bg-amber-500"><div className="container-site flex flex-col justify-between gap-6 py-10 lg:flex-row lg:items-center"><div><h2 className="text-2xl font-extrabold tracking-tight text-navy-950 sm:text-3xl">{title}</h2><p className="mt-2 max-w-2xl text-sm font-semibold leading-6 text-navy-800">{description}</p></div><div className="flex flex-col gap-3 min-[430px]:flex-row"><Link href={bookingHref} className="btn btn-navy"><CalendarCheck size={18}/>Book taxi</Link><a href={phoneHref} className="btn border border-navy-900/20 bg-white/70 text-navy-950"><Phone size={18}/>Call now</a></div></div></section>; }
