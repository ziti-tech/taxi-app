import Link from "next/link";
import { ArrowRight, MapPin, Route } from "lucide-react";
import type { TaxiRoute } from "@/types/content";
export function RouteCard({ route }: { route: TaxiRoute }) { const book = `/book?pickup=${encodeURIComponent(route.origin)}&drop=${encodeURIComponent(route.destination)}${route.airportRoute ? "&tripType=airport" : ""}`; return <article className="flex min-w-0 flex-col rounded-2xl border border-line bg-white p-5 shadow-[var(--shadow-card)]"><div className="flex items-center justify-between"><span className="grid size-10 place-items-center rounded-xl bg-navy-950 text-amber-500"><Route size={19}/></span><span className="rounded-full bg-warm-50 px-2.5 py-1 text-[10px] font-extrabold uppercase tracking-wider text-muted">{route.airportRoute ? "Airport" : route.category.replace("-", " ")}</span></div><div className="mt-6 grid grid-cols-[minmax(0,1fr)_auto_minmax(0,1fr)] items-center gap-2"><div className="min-w-0"><p className="text-[10px] font-bold uppercase text-muted">From</p><h2 className="break-words font-extrabold text-navy-950">{route.origin}</h2></div><ArrowRight className="text-amber-600" size={18}/><div className="min-w-0"><p className="text-[10px] font-bold uppercase text-muted">To</p><p className="break-words font-extrabold text-navy-950">{route.destination}</p></div></div><p className="mt-4 flex-1 text-sm leading-6 text-muted">{route.shortDescription}</p><p className="mt-4 flex items-center gap-2 text-xs font-extrabold text-navy-900"><MapPin size={15}/>Fare to be confirmed</p><div className="mt-5 grid grid-cols-2 gap-2"><Link href={book} className="btn btn-primary px-3 text-sm">Book taxi</Link><Link href={`/routes/${route.slug}`} className="btn border border-line bg-white px-3 text-sm text-navy-900">View route</Link></div></article>; }
