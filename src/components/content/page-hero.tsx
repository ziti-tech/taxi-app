import Link from "next/link";
import { CalendarCheck, Phone } from "lucide-react";
import { phoneHref } from "@/config/site";
import { Breadcrumbs, type BreadcrumbItem } from "@/components/shared/breadcrumbs";
export function PageHero({ breadcrumbs, eyebrow, title, description, bookingHref = "/book", bookingLabel = "Book taxi", secondary = true }: { breadcrumbs: BreadcrumbItem[]; eyebrow: string; title: string; description: string; bookingHref?: string; bookingLabel?: string; secondary?: boolean }) { return <section className="bg-navy-950 text-white"><div className="container-site py-10 md:py-14 lg:py-16"><div className="[&_a]:text-white/60 [&_span]:text-white/80"><Breadcrumbs items={breadcrumbs}/></div><div className="mt-8 max-w-3xl"><p className="eyebrow text-amber-500">{eyebrow}</p><h1 className="mt-3 text-3xl font-extrabold tracking-tight sm:text-4xl lg:text-5xl">{title}</h1><p className="mt-5 max-w-2xl text-base leading-7 text-white/70 md:text-lg">{description}</p><div className="mt-7 flex flex-col gap-3 min-[430px]:flex-row"><Link href={bookingHref} className="btn btn-primary"><CalendarCheck size={18}/>{bookingLabel}</Link>{secondary && <a href={phoneHref} className="btn btn-secondary"><Phone size={18}/>Call for fare</a>}</div></div></div></section>; }
