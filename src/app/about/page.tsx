import type { Metadata } from "next";
import { Check, MapPin, MousePointerClick, ShieldCheck } from "lucide-react";
import { PageHero } from "@/components/content/page-hero";
import { FinalBookingCta } from "@/components/content/final-booking-cta";
import { siteConfig } from "@/config/site";
import { createMetadata } from "@/lib/metadata";
export const metadata: Metadata = createMetadata({ title: "About Villupuram Taxi", description: "Learn about the approach behind this Villupuram taxi booking service, its service-area focus and simple request process.", path: "/about" });
export default function AboutPage() { const principles = [{ title: "Simple booking", text: "Enter the trip, choose a vehicle and review the request without creating an account.", icon: MousePointerClick }, { title: "Clear information", text: "Unconfirmed rates and policies are identified rather than presented as established facts.", icon: Check }, { title: "Practical trust", text: "Safety and reliability standards remain configurable until the operator approves exact claims.", icon: ShieldCheck }]; return <main id="main-content"><PageHero breadcrumbs={[{ label: "Home", href: "/" }, { label: "About" }]} eyebrow="About the service" title="A simpler way to request a taxi from Villupuram" description="This website is being built around straightforward trip planning, readable fare status and an easy path to operator confirmation."/><div className="container-site section-space"><section className="grid gap-10 lg:grid-cols-[1.1fr_.9fr]"><div><p className="eyebrow">Who we are</p><h2 className="section-title mt-3 text-navy-950">Local identity, pending final client details</h2><p className="mt-5 leading-7 text-muted">{siteConfig.businessName} is currently a temporary configured identity. Final business history, credentials and operating claims will be added only after the client supplies and approves them.</p></div><div className="rounded-2xl bg-navy-950 p-6 text-white"><MapPin className="text-amber-500"/><h2 className="mt-4 text-xl font-extrabold">Where we serve</h2><p className="mt-3 text-sm leading-6 text-white/65">{siteConfig.serviceArea}. Exact pickup coverage and route availability are confirmed for each request.</p></div></section><section className="mt-16"><p className="eyebrow">Our approach</p><h2 className="section-title mt-3 text-navy-950">Designed around the booking task</h2><div className="mt-7 grid gap-5 md:grid-cols-3">{principles.map(({ title,text,icon:Icon }) => <article key={title} className="rounded-2xl border border-line bg-white p-6"><Icon className="text-amber-600"/><h3 className="mt-4 font-extrabold text-navy-950">{title}</h3><p className="mt-2 text-sm leading-6 text-muted">{text}</p></article>)}</div></section></div><FinalBookingCta/></main>; }
