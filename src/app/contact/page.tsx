import type { Metadata } from "next";
import { Clock3, Mail, MapPin, MessageCircle, Phone } from "lucide-react";
import { PageHero } from "@/components/content/page-hero";
import { FinalBookingCta } from "@/components/content/final-booking-cta";
import { phoneHref, siteConfig, whatsappHref } from "@/config/site";
import { createMetadata } from "@/lib/metadata";
export const metadata: Metadata = createMetadata({ title: "Contact Villupuram Taxi", description: "Contact the Villupuram taxi operator or start a booking request for local, airport, one-way and outstation travel.", path: "/contact" });
export default function ContactPage() { const contacts = [{ label: "Phone", value: siteConfig.phone, href: siteConfig.phone ? phoneHref : null, icon: Phone }, { label: "WhatsApp", value: siteConfig.whatsapp, href: whatsappHref, icon: MessageCircle }, { label: "Email", value: siteConfig.email, href: siteConfig.email ? `mailto:${siteConfig.email}` : null, icon: Mail }, { label: "Address", value: siteConfig.address, href: null, icon: MapPin }, { label: "Business hours", value: siteConfig.businessHours, href: null, icon: Clock3 }]; return <main id="main-content"><PageHero breadcrumbs={[{ label: "Home", href: "/" }, { label: "Contact" }]} eyebrow="Contact" title="Plan your taxi from Villupuram" description="Start with the booking form or use an approved contact channel when available. Missing client details are intentionally not replaced with fake information."/><div className="container-site section-space"><section><h2 className="section-title text-navy-950">Contact information</h2><div className="mt-7 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{contacts.map(({ label,value,href,icon:Icon }) => <article key={label} className="rounded-2xl border border-line bg-white p-5"><Icon className="text-amber-600"/><h3 className="mt-4 text-xs font-extrabold uppercase tracking-wider text-muted">{label}</h3>{value ? href ? <a href={href} className="mt-2 block break-words font-extrabold text-navy-950 underline decoration-amber-500 underline-offset-4">{value}</a> : <p className="mt-2 break-words font-extrabold text-navy-950">{value}</p> : <p className="mt-2 text-sm font-bold text-muted">To be supplied by client</p>}</article>)}</div></section><section className="mt-14 rounded-2xl border border-dashed border-line bg-white p-7 text-center"><MapPin className="mx-auto text-amber-600"/><h2 className="mt-4 text-xl font-extrabold text-navy-950">Map available after location approval</h2><p className="mx-auto mt-2 max-w-xl text-sm leading-6 text-muted">A verified business location or service-area map can be connected later. No placeholder map is embedded.</p></section></div><FinalBookingCta/></main>; }
